[target.x86_64-unknown-linux-gnu]
# Use system's default linker (or install mold for faster linking)
# sudo apt install mold
# rustflags = ["-C", "link-arg=-fuse-ld=mold"]

[build]
incremental = true
